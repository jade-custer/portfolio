2025

These sets of codes focuses on converting C code to RISC-V assembly code. Some code was simpler than others, but this was the code that taught me RISC-V and how assembly code breaks down more sophisticated higher level code into multiple different steps. Everything from shifting, bitwise logic, loading, storing, jumping, branching, and memory management